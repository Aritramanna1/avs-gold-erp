/**
 * Platform tax-invoice print store stub — full reconstruction pending shop print chunk parity.
 */
export function usePlatformPrintStore() {
  return {
    templates: [] as unknown[],
    hydrate: async () => undefined,
  };
}
