# AVS Gold ERP — Owner Handover (2026-09-12)

**Audience:** owner / next eng session after Cursor usage limits.  
**Written by:** Lingxi's Engineer Bot (eng) · Projects Manager owns product scope while owner is away.  
**No secret values in this file.** Env var *names* only.

---

## 1. Mission

Ship **AVS Gold ERP** (firm accounts **222 / 777**) on Hostinger-first architecture: custom email + heavy work on Hostinger; **Supabase = auth/DB only** (free plan — no new Edge Functions, no mail, no chatty loops). Complete open Agent Tasks + AI automation capabilities. **Local laptop only** until owner green-lights Hostinger pull.

---

## 2. Accounts (no passwords)

| Role | Identifier | Notes |
|------|------------|--------|
| Owner / GitHub | Aritramanna1 | Merges authorized for eng as of 2026-09-12 |
| Firm accounts | **222**, **777** | Product tenants — no credentials here |
| Workspace Notion | Aritra Manna’s Workspace | Fleet + Agent Tasks |
| SMTP send-from | `projects@arivahly.in` | Hostinger mailbox — **password not in repo/chat/Notion** |
| SMTP host | `smtp.hostinger.com:465` | Env: `SMTP_HOST`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM_*` |

Credentials live only in **local untracked `.env`** / Hostinger secrets. Never paste values into Notion, PRs, or chat.

---

## 3. Local tip path

```
C:\final erp 29.08\new and final
```

- Machine: **Maa_Tara**
- GitHub: `https://github.com/Aritramanna1/avs-gold-erp`
- `origin` may dual-push (github + `avs-erp-hostinger-live`). Prefer github.com-only for PR branch pushes when the Hostinger mirror lease rejects.
- Tip `main` after handover merges includes PR **#38** (legal mojibake). Confirm with `git fetch origin` / `git log -1 origin/main`.

---

## 4. Merged PRs this burst (#25–#38)

| PR | Topic | Notes |
|----|--------|--------|
| #25 | RATE-01 rates/bhav + demo `CONTAMINATED_SHOP_NAMES` | `547dfee` |
| #26 | DOCS print masters + MASTER README | |
| #27 | Credits jargon + zero-burn; attention VIEW; tawk prep | |
| #28 | SETTINGS-01 flat→nested firm keys | |
| #29 | MVP-STOCK Gold-first | |
| #30 | SEC untrack env secrets + scrub dumps (AVS-73) | Owner still rotate any exposed creds |
| #31 | Hostinger SMTP pack (EM-01 / AUTH-MAIL) | No secrets in pack |
| #32 | AVS-67 NL `openRoute` Sell/Stock/Make/Money | |
| #33 | AVS-71 GetParty360 real dossier | |
| #34 | AVS-70 EXECUTE payment/settlement confirm→commit | |
| #35 | AVS-68 create_task + AVS-69 recommend_reorder | |
| #36 | Tax profile + CA `ca_approved_at` gate | |
| #37 | MVP-HOME + RATE-CTA (+ partial AVS-57) | |
| #38 | Legal mojibake — `/privacy` `/terms` from canonical copy | `ddffbd8` |

**Open GitHub PRs after #38:** none (as of 2026-09-12 ~06:02Z).

---

## 5. LIVE ≠ tip

Post-merge smoke (**AVS-74 Done**):

- Live erp `/login` chrome **PASS**; `health` ~`1.1.2` healthy.
- Hostinger HTML **Last-Mod Fri 11 Sep** — **not** today’s tip.
- **No GOLD RATE header live** until Hostinger pull of tip (#37).
- Aurum: `ERR_SSL_PROTOCOL_ERROR`.
- Portal + aurumportal: **NXDOMAIN**.
- Legal mojibake was on tip routes; fixed in **#38** (still needs Hostinger pull to show live).

**OPS:** Hostinger pull of **main tip → erp** is boarded as owner-only. **NOT shop** (`maatarajewellers.shop`). Hold until owner explicitly says deploy/pull.

---

## 6. SMTP / AUTH-MAIL

- Send-from: `projects@arivahly.in` via Hostinger.
- Env names only: `SMTP_HOST`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM_*`.
- Local SMTP auth test was OK earlier; mailbox password was exposed in chat historically → **owner must rotate** in hPanel, then refresh **local `SMTP_PASS` only**.
- PR **#31** SMTP pack is on `main`. **AUTH-MAIL** app cutover (forgot-password / OTP off Supabase `send-email` invoke) still **blocked** on rotate + owner go.
- Do **not** hardcode passwords in repo/Notion/PRs.

---

## 7. Human blockers (owner)

1. **Rotate** `projects@` mailbox password → update local `SMTP_PASS` only.
2. **hPanel:** aurum SSL + portal / aurumportal DNS.
3. **Hostinger pull** main tip → **erp** (explicit go) — **not** shop.
4. Optional: re-smoke after pull (Researchy / AVS-74 pattern).

---

## 8. Research Done (high level)

Attention, support stack, email/ticket maps, live smoke, AI capabilities matrix, credential audit, low-literacy gaps, AVS-74 post-merge chrome (with LIVE≠tip caveats). WhatsApp credit UX stubs Done via #27.

---

## 9. Open Notion / next eng work

**Still In progress / Holding (do not mark Done until unblocked):**

- **EM-01** — Platform email path (pack #31 merged; needs SMTP rotate).
- **AUTH-MAIL** — OTP/reset via Hostinger email (cutover after rotate).
- Fleet **Holding:** aurum SSL; portal/aurumportal DNS; OPS Hostinger pull (owner gate).

**Likely next after owner gates:** AUTH-MAIL cutover on laptop → scheduled-jobs expiry mail (T-14/T-7/T-1, idempotent) → remaining MVP (SELL/DAYCLOSE, full MVP-NAV) → Chatwoot VPS (blocked) → support→Notion bridge later.

Refresh Agent Tasks / Fleet in Notion — many IMPL rows for #26–#37 already marked **Done**.

---

## 10. Standing rules

1. **No Cursor cloud agents** for this repo (local laptop / Maa_Tara).
2. **No hardcoded credentials** — env / Hostinger secrets / `.env.example` placeholders only.
3. **No shop deploy** to `maatarajewellers.shop`.
4. **No Hostinger erp pull** until owner explicit go.
5. Supabase = auth/DB only; Hostinger for email + heavy work.
6. Eng may merge GitHub PRs when owner authorized (2026-09-12); still no auto-deploy.

---

## 11. Quick resume checklist

1. `cd "C:\final erp 29.08\new and final"` → `git fetch origin` → `git log -1 origin/main`
2. Confirm open PRs empty: `gh pr list --repo Aritramanna1/avs-gold-erp --state open`
3. If owner rotated SMTP: set local `SMTP_PASS` → AUTH-MAIL cutover
4. If owner said pull: Hostinger sync tip → erp (not shop) → re-smoke GOLD RATE / #37 / #38
5. Never paste secrets into Notion or this handover

---

*End of handover. Generated 2026-09-12 for usage-limit continuity.*
