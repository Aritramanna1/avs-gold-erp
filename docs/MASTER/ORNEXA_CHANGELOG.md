# Ornexa Product Memory Changelog

## 2026-08-13

- Created permanent master product memory structure.
- Seeded constitution, product spec, business rules, workflows, data/ledger model, roles, configuration, integrations, security, legacy compatibility, implementation matrix, decision log, and changelog.
- Updated root AGENTS.md with the requirement to read master product memory before modifying Ornexa / AVS.
- Added `src/lib/ornexa/transaction-contracts.ts` as the first code anchor for the universal AVS transaction lifecycle.
- Improved startup behavior so the ERP shell paints progressively while license/platform checks and non-critical chrome load in the background.
- Added `ORNEXA_A_TO_Z_MASTER.md` as the single full-scope master checklist for product completion.
- Added Checklist Design Web App compliance as a release quality gate.
- Added centralized ERP architecture rule: one party, user/role, firm/branch, metal, transaction, inventory, manufacturing, accounting, document, storage, notification, audit, reporting, and assistant foundation.
- Added `docs/ORNEXA_WEBAPP_COMPLETENESS_CHECKLIST.md`.
- Added `docs/JEWELLERY_ERP_REFERENCE_MATRIX.md`.
- Added `docs/MASTER/ORNEXA_PROGRAMME_DASHBOARD.md` to coordinate specialist workstreams and completion evidence.
- Recorded Product Owner decision that Supabase online is the non-negotiable production architecture; older offline/Hybrid docs are legacy/business-rule references only.
- Added `docs/MASTER/ORNEXA_MASTER_AUDIT_2026-08-13.md` with the current master audit answer, centralized architecture status, jewellery ERP reference classification, Checklist.Design gate status, and release blockers.
- Expanded the master audit with explicit web-app/admin/comms/intelligence gaps: account access, pricing, admin panel, RBAC, organization settings, maintenance, API keys, danger zone, audit mode, timelines, chats, message import, read receipts, files, and assistant intelligence.
- Added Supabase-only central foundation migration `20260813001000_ornexa_central_foundation_party_timeline_comms_ai.sql` for Party 360, party roles/contacts, activity timeline, message threads/messages/read receipts/files, API key registry metadata, maintenance windows, and assistant action audit.
- Added Product Owner review notes: Attendance must support bulk add/update later, and jewellery book/module navigation must preserve the app shell with sectional skeletons instead of full-page dead refreshes.
- Implemented the first bulk daily attendance path in the Attendance & Salary route using Supabase-backed `attendance` upserts; documented remaining range/mobile/audit verification before release PASS.
- Applied Supabase migration `20260813024500_central_foundation_rls_performance_tuning.sql` to add central foundation FK indexes and split maintenance-window write policies away from SELECT.
- Removed the browser-local support ticket queue from `platform-support-service`; error-screen ticket/live-chat actions now use the Supabase support path or fail visibly.
- Applied Supabase migrations `20260813031000_online_operational_state_tables.sql`, `20260813032500_online_device_and_reconciliation_state.sql`, and `20260813034000_security_operations_online.sql`.
- Removed the retired browser `local-db.ts` and `local-file-store.ts` infrastructure from active code.
- Moved print job history, scheduler state, escalation state, device registry, gold reconciliation reports, attachment bytes, Wasender webhook history, backup requests, DR drill records, and key-rotation requests to Supabase-backed tables/services.
- Verified `npx tsc --noEmit --pretty false` and `npm run build` after the Supabase-only migration sweep. Build still reports chunk-size and ineffective dynamic-import warnings that remain performance follow-ups, not compile failures.
- Removed document-numbering and generic sequence local fallbacks; legal/business document numbers now require the Supabase `next_document_number` RPC and fail visibly if a number cannot be reserved.
- Removed localStorage persistence from branch access state, communication provider settings, workflow engine config, manufacturing bill state, automation settings, and reference notes.
- Added Supabase migration `20260813041000_reference_notes_online.sql` for firm-scoped reference notes with RLS.
- Set the default workflow preset to manufacturing-only and made workflow/communication/automation settings hydrate from Supabase during boot.
- Retired local billing module persistence and local pilot import/export restore paths; browser-local business backup is no longer a supported data path.
- Moved weekly statement opt-in and backup scheduler configuration/last-run state to Supabase-backed `app_settings`.
- Added Supabase hardening migrations `20260813043000_rpc_and_rls_hardening.sql` and `20260813043500_function_search_path_hardening.sql`: anonymous RPC execution is revoked, no-policy security tables have RLS policies, and `is_valid_feature_key(text)` has a pinned search path.
- Added Supabase hardening migration `20260813044000_internal_rpc_execution_hardening.sql` to revoke direct signed-in Data API execution for internal trigger/maintenance functions that are not frontend RPC workflows.
- Applied Supabase hardening migrations `20260813044500_unused_rpc_surface_hardening.sql` and `20260813045000_service_rpc_surface_hardening.sql`: unused/internal signed-in RPC execution was revoked for maintenance, provisioning, universal posting, platform settings, tenant onboarding, raw license validation, and platform billing helper functions.
- Verified the remaining signed-in `SECURITY DEFINER` RPC surface is 31 functions, limited to current product RPC workflows and RLS/module helper functions pending per-workflow least-privilege review.
- Applied Supabase RLS tightening migration `20260813045500_public_policy_role_tightening.sql`; follow-up SQL checks show no public tables without RLS, no RLS-enabled public tables without policies, and no remaining public-schema policies targeted to `{public}`.
- Cleared TanStack route code-splitting browser warnings from 15 to 0 by removing unnecessary route component exports and reusing embedded WhatsApp settings components through route configuration.
- Switched Supabase Auth persistence to browser `sessionStorage` so sessions persist during the active browser session and expire when the browser/tab session closes; support REST fallback now reads the session token from `sessionStorage` first.
- Retired the last active local-pilot migration path: `src/lib/cloud-migrate.ts` was deleted, Daily Close no longer blocks print on local-to-cloud migration, and Settings now directs backup/restore to Supabase Backup & Disaster Recovery.
- Rewrote `docs/ARCHITECTURE.md`, `docs/DATABASE.md`, and `docs/V1_RELEASE_NOTES.md` so older Offline/Hybrid guidance is clearly superseded by the Supabase-online production architecture.
- Removed redundant root startup preloads for communication queue and bullion rate polling, cleared the remaining ineffective dynamic-import build warnings, and verified `npx tsc --noEmit --pretty false`, `npm run build`, and `http://127.0.0.1:3000/` HTTP 200.
- Changed generic form draft persistence from long-lived browser `localStorage` to browser-session `sessionStorage`; submitted orders and business records remain Supabase-authoritative.
- Changed the browser license cache/manual-entry helper to browser-session storage; tenant license authority continues to resolve through Supabase RPC/licensing validation.
- Removed user-facing "Offline Mode" recovery guidance from normalized network errors; connection failures now direct users to retry online or create a support ticket.
- Removed active offline/local helper guards and repository aliases from the Supabase data path: `data-loader`, branch/workshop sync, approval workflow, and generic repositories now use explicit Supabase-online operations instead of `isOfflineMode`, `saveLocal`, `getAllLocal`, or `updateLocal` compatibility methods.
- Cleaned active storage, attachment, print, PDF, WhatsApp-document, security-center, and storage-diagnostics copy/comments so they describe Supabase-backed document storage and retired local-vault/key behavior instead of implying an Offline/Hybrid/local database production path; verified `npx tsc --noEmit --pretty false` and `npm run build`.
- Rechecked Supabase via MCP after the cleanup: public tables with RLS disabled = 0 of 129, RLS-enabled tables without policies = 0; remaining advisor work is signed-in callable `SECURITY DEFINER` review, leaked-password protection, unindexed foreign keys, and multiple-permissive-policy performance cleanup.
- Added and remotely applied `20260813050000_internal_helper_rpc_tightening.sql`, revoking direct signed-in Data API execution for internal helper RPCs `apply_plan_entitlements(uuid, uuid, text)` and `require_organization_feature(text)` while preserving service-role execution. Verified signed-in callable `SECURITY DEFINER` surface reduced from 33 to 31, RLS remains 0/129 public tables without RLS and 0 RLS tables without policies, `npx tsc --noEmit --pretty false`, `npm run build`, HTTP 200, and Playwright startup smoke with no console issues.
- Added `docs/MASTER/ORNEXA_SUPABASE_RPC_SECURITY_MATRIX.md` classifying the remaining 31 signed-in callable `SECURITY DEFINER` functions as intentional endpoints or RLS helpers with next actions, so future hardening does not break login, licensing, support, document sharing, tenant isolation, or karigar portal workflows.
- Added and remotely applied `20260813051000_missing_foreign_key_indexes.sql`, creating deterministic covering indexes for public foreign keys that did not already have one. Supabase MCP verification shows missing public FK indexes = 0, generated FK indexes = 83, public tables without RLS = 0 of 129, and RLS-enabled tables without policies = 0. Re-verified `npx tsc --noEmit --pretty false`, `npm run build`, HTTP 200, and Playwright route smoke for `/`, `/people`, `/attendance`, `/platform?view=licensing`, and `/platform?view=settings` with no blank/database-error/console-error result.
- Added and remotely applied `20260813052000_merge_overlapping_select_policies.sql`, merging the remaining overlapping permissive SELECT policy pairs for `organization_usage_snapshots` and `platform_notifications` while preserving the same tenant/SaaS-admin and recipient/SaaS-admin access semantics. Supabase MCP verification shows overlapping permissive policy groups = 0, missing public FK indexes = 0, public tables without RLS = 0 of 129, and RLS-enabled tables without policies = 0. Re-verified `npx tsc --noEmit --pretty false`, `npm run build`, HTTP 200, and the same Playwright route smoke.
- Hardened customer-side support actions in `src/lib/platform-support-service.ts`: browser ticket creation now uses the staff-support RPC first, then falls back to Supabase REST/table insertion under the same firm/requester RLS constraints, and finally to the client provider path; browser ticket listing now also reaches the RPC fallback instead of failing after one REST path. Verified `npx tsc --noEmit --pretty false` and `npm run build`; unauthenticated Playwright support-route smoke shows no blank/database-error/console-error result, while live ticket creation still requires a valid authenticated E2E profile before PASS.
- Moved platform notification read state toward Supabase authority: `src/lib/notifications-store.ts` now loads `platform_notifications`, treats rows with `read_at` as read, and updates `read_at` when a platform notification is opened or all platform notifications are marked read. Computed operational alerts may still use browser read IDs, but platform-delivered notifications no longer rely only on `localStorage`. Updated `src/routes/notifications.tsx` to render Supabase-authored notification titles/bodies. Verified `npx tsc --noEmit --pretty false`, `npm run build`, HTTP 200 for `/notifications`, and unauthenticated Playwright route smoke with no blank/database-error/console-error result; authenticated read/write evidence remains required before PASS.
- Completed another Supabase-only documentation/source hygiene sweep: retired Offline/Hybrid installer, pilot, notification, settings, attachment-disclosure, final-verification, troubleshooting, and hybrid setup instructions now point to Supabase-online authority or are clearly labeled historical; active source comments in auth, session lock, material vault, barcode, settlement, outside work, and Supabase cache paths no longer describe local-first authority. Updated master audit/A-to-Z/support evidence to reflect the 31-function RPC surface and Supabase-backed support state. Verified `npx tsc --noEmit --pretty false`, `npm run build`, HTTP 200, clean active helper grep for `saveLocal|getAllLocal|updateLocal|isOfflineMode|cloud-migrate|local-file-store|local-db`, and Playwright route smoke for `/`, `/people`, `/attendance`, `/notifications`, `/platform?view=licensing`, and `/platform?view=settings` with no blank/database-error/console-error result.
- Tightened the no-local-storage rule from the master docs: theme/language preference, what-is-new dismissal, computed notification read markers, renderer error logs, E2E seed cache, and device id now use browser `sessionStorage` instead of `localStorage`; support REST fallback no longer reads auth/config from `localStorage`; print setup is runtime-only and no longer uses Zustand `persist`; obsolete `SUPABASE_RUNTIME_KEYS` exports were removed. Verified `rg "localStorage" src` shows only retired residue deletion in `clearPilotData`, `npx tsc --noEmit --pretty false`, `npm run build`, and Playwright route smoke for `/`, `/settings/workflow`, `/settings/support`, and `/notifications`.
- Removed the final active `localStorage` API references from source: generic form drafts are now runtime-only until backed by a Supabase draft table/RPC, the retired `clearPilotData` browser-local cleanup function was removed, settings imports were cleaned, and the settings backup UI copy now points admins to Supabase recovery instead of implying browser-local restore is supported. Verified `rg "localStorage|clearPilotData|createJSONStorage|zustand/middleware" src` returns no matches and `npx tsc --noEmit --pretty false` passes.
- Rewrote high-risk public operator docs that still described Hybrid/offline/local recovery as current guidance: `docs/ADMINISTRATOR_GUIDE.md`, `docs/BACKUP_RESTORE_GUIDE.md`, `docs/SECURITY.md`, `docs/STARTUP_PERFORMANCE.md`, and `docs/README.md` now point to Supabase-online operations, Supabase recovery, Supabase Auth/RLS, and staged online loading. Cleaned active source wording/types that could reintroduce local authority: `src/lib/db-status.ts` now exposes only `supabase_online`, Settings no longer reads deployment mode for local/online branches, and startup/data-loader/security/print comments no longer describe Offline/SQLite as active.
- Extended the global command/search layer without adding browser-local persistence: `src/lib/global-search.ts` now adds a Supabase/RLS-backed remote search path for central parties, branches, inventory, invoices, platform billing documents, and manufacturing barcode/tag identities, and `src/components/GlobalCommandPalette.tsx` merges those results with the fast loaded-store search while preserving route-permission filtering. Verified `npx tsc --noEmit --pretty false` and `npm run build`; authenticated browser/RLS no-leak evidence is still required before the Checklist Search gate can move from PARTIAL to PASS.
- Wired central-party search deep links into the People workspace: `/people?selected=<personId>` now selects the matching People profile, and `/people?central=<centralPartyId>` resolves the Supabase `central_parties.metadata.source_id` back to the existing profile instead of creating a duplicate party UI. This preserves the centralized Party foundation while the fuller Party 360 page remains a release blocker. Verified `npx tsc --noEmit --pretty false`.
- Wired manufacturing tag search links into the barcode workspace: `/barcode?selected=<manufacturingBarcodeId>` now displays selected tag identity, order/customer/status/weight context, and opens the existing `ManufacturingTagPrintDialog` for jewellery tag/barcode/QR printing instead of routing manufacturing barcodes into the stock-item printer. Verified `npx tsc --noEmit --pretty false`.
- Added `src/components/web-app-state.tsx` as a reusable Checklist.Design-aligned state primitive for empty/filtered-empty/saving states without browser-local persistence. Applied it to the central People/KYC selected states and Ready Stock empty state, and made Ready Stock table rows deep-link to item detail. Verified `npx tsc --noEmit --pretty false`; ERP-wide empty/table coverage remains incomplete.
- Extended the reusable web-app state layer with `FormStatus` and `FieldError`, then upgraded Ready Stock Entry with inline validation, required-field feedback, gram unit validation, in-form saving status, and server-error feedback while preserving the existing Supabase-backed stock/vault/barcode save path. Verified `npx tsc --noEmit --pretty false`; ERP-wide form and saving coverage remains incomplete.
- Rechecked the Product Owner no-local-storage directive against active source and tightened old reset wording: `clearAllLocalData` is now `clearBrowserSessionResidue`, Settings Danger Zone and the reset dialog now describe browser-session cleanup only, and no UI claims a local ERP reset/import/export path. Verified `rg "localStorage|clearAllLocalData|Factory Reset App|Erase All Data|local browser reset|Erasing" src` returns no active matches, plus `npx tsc --noEmit --pretty false` and `npm run build`.
- Began the master large-data strategy implementation without adding local/offline storage: added `src/lib/stock-query.ts` for Supabase/RLS range-count inventory queries and rewired `src/routes/stock.index.tsx` into a URL-backed stock register with search, status filter, pagination, loading/error/filtered-empty states, and row detail links. Updated the A-to-Z master large-data row from MISSING to PARTIAL and the Checklist table gate from FAIL to PARTIAL for this route. Verified `npx tsc --noEmit --pretty false`, `npm run build`, and unauthenticated `/stock?q=test&status=available&page=1` route smoke with no page/console errors.
- Extended the large-data strategy to People without adding browser-local storage: added `src/lib/people-query.ts` for Supabase/RLS range-count People queries and rewired main People tabs to URL-backed search/tab/page state, per-tab counts, loading/error states, and page controls. Bounded legacy People hydration in `src/lib/data-loader.ts` and `src/lib/people-store.ts` to 250 recent rows for compatibility/detail helpers so startup no longer hydrates thousands of parties by default. Verified `npx tsc --noEmit --pretty false`, `npm run build`, and unauthenticated `/people?q=rahul&tab=karigars&page=1` route smoke with no page/console errors; authenticated high-volume timing evidence remains required.
- Rechecked the Product Owner instruction not to implement browser-local storage while stabilizing People pagination: active source still has zero `localStorage`, `clearAllLocalData`, `createJSONStorage`, or `zustand/middleware` matches. Paginated People row actions now seed only the bounded in-memory compatibility store before calling the existing Supabase-backed update/delete methods, so action buttons do not depend on a full client-side People hydrate. Verified `npx tsc --noEmit --pretty false` and `npm run build`; remaining high-volume Supabase bulk loaders are tracked under the Large data strategy gate rather than hidden as completed work.
- Removed the platform owner console's 10k-row invoice/order/job-card summary hydration. Added `supabase/migrations/20260813053000_platform_firm_stats_rpc.sql` with `get_platform_firm_stats()` and `src/lib/platform-stats-query.ts`, then wired `src/routes/platform.tsx` to use the aggregate RPC with a bounded exact-count fallback. Verified `npx tsc --noEmit --pretty false`, `npm run build`, and active-source no-local-storage grep. Supabase CLI is not available in this shell, so remote RPC application and authenticated runtime evidence remain pending before this platform stats item can be marked PASS.
- Replaced hard-coded assistant demo responses in `src/lib/avs-assistant-brain.ts` with deterministic Supabase/RLS-backed tools for customer balance context, karigar gold book summaries, daily operational follow-ups, and draft gold issue cards. High-risk gold issue remains draft-only and records an `assistant_action_audit` suggestion when the central audit table is available. Verified `npx tsc --noEmit --pretty false`, `npm run build`, and grep confirmed the old fabricated Ramesh/overdue demo responses are gone. Remaining assistant work before PASS: authenticated no-leak tests, confirmation execution flow, broader ERP tool coverage, provider gateway hardening, and voice UI.
- Bounded `src/lib/orders-store.ts` refresh to a 500-row recent compatibility/detail cache ordered by `updated_at`, instead of hydrating 10,000 orders into the browser. This does not complete the Orders register; route-level Supabase pagination and `data-loader` order hydration remain tracked under the Large data strategy gate. Verified `npx tsc --noEmit --pretty false`.
- Replaced the remaining 5k/10k/20k background hydrations in `src/lib/data-loader.ts` with named startup compatibility-cache limits: 500 operational/detail rows, 1000 ledger/reference rows, and 1000 attachment metadata rows. The loader remains Supabase-online only and does not add browser-local persistence. Verified `npx tsc --noEmit --pretty false`, active-source no-local-storage grep, `data-loader.ts` bulk-limit grep, and HTTP 200 for `/`; individual stores such as billing, job cards, ledger, stock, worker gold book, and workers still need the same treatment.
- Replaced the remaining detected 5k/10k/20k individual store refreshes with bounded Supabase compatibility-cache reads: billing invoices, job cards, stock inventory/movements, melt jobs, gold ledger, worker gold book, attendance, worker transactions, worker settlements, and salary rules now use 500/1000-row limits with server ordering where available. This removes the current large client hydrate pattern without adding browser-local or offline storage. Verified `rg "limit\\(10000\\)|limit\\(20000\\)|limit\\(5000\\)|localStorage|clearAllLocalData|createJSONStorage|zustand/middleware" src\\lib src\\routes src` returns no matches, `npx tsc --noEmit --pretty false`, and `npm run build`. Balance-critical ledger/worker history still requires Supabase aggregate/RPC-backed full-balance queries before release PASS.
- Added `src/lib/billing-query.ts` and rewired the Billing invoice register to use Supabase range/count pagination with URL-backed search/page state, loading/error/empty states, and server-side invoice totals instead of rendering the main invoice table from the compatibility cache. Added `supabase/migrations/20260813054000_billing_outstanding_rpc.sql` with the RLS-respecting `get_billing_outstanding_summary(text)` RPC, plus a bounded fallback, and wired the Billing Outstanding tab to that Supabase-backed summary. Verified `npx tsc --noEmit --pretty false`, `npm run build`, active-source no-local/bulk-limit grep, and unauthenticated Playwright smoke for `/billing?q=test&page=1` with no blank/database-error/console-error result. Billing customer-ledger tab still depends on bounded compatibility cache/ledger compilers and needs aggregate or route-specific Supabase queries before Billing can pass the table/release gate.
- Added `src/lib/customer-ledger-context.ts` and wired Billing's customer ledger tab to hydrate only the selected customer's Supabase/RLS context before compiling the existing unified ledger: invoices, orders, customer gold settlements, job cards, and delivery challans are fetched by customer instead of relying on a tenant-wide browser hydrate. The ledger panel now has loading and recoverable error states. This is still PARTIAL, not release PASS: manufacturing bills and future ledger dimensions need inclusion, authenticated customer-ledger correctness/no-leak tests are pending, and full historical balance reporting still needs aggregate/RPC-backed evidence.
- Added `src/lib/orders-query.ts` and rewired the Orders register to use Supabase/RLS range/count pagination with URL-backed search, status, type, and page state. The route now shows loading, recoverable error, filtered-empty, and paginated table states instead of filtering the compatibility cache in React. Verified `npx tsc --noEmit --pretty false`; remaining before PASS: richer indexed search across customer/item text, authenticated high-volume timing/no-leak evidence, mobile table strategy, exports, and related manufacturing/report route pagination.
- Expanded `src/lib/avs-assistant-brain.ts` with two additional deterministic, read-only Supabase/RLS assistant tools: production queue intelligence over open job cards and inventory exception intelligence over visible stock/rate/reserved/available-fine data. These tools write best-effort `assistant_action_audit` rows like the existing assistant tools and do not post business transactions. Verified `npx tsc --noEmit --pretty false`; assistant remains PARTIAL pending authenticated no-leak tests, provider gateway hardening, confirmation execution flow, voice UI, and broader document/accounting tools.
- Added `src/lib/manufacturing-query.ts` and rewired the Manufacturing workspace landing route to load open job-card count, bill counts, draft/finalised counts, and recent manufacturing bills through Supabase/RLS exact-count and bounded recent-row queries. The route now has loading, recoverable error, and empty states instead of depending on full manufacturing bill/job-card store refreshes. Verified `npx tsc --noEmit --pretty false`; full manufacturing graph PASS still requires order-to-job-to-issue-to-receive-to-QC-to-hallmark-to-stock-to-settlement E2E proof and deeper report pagination.
- Extended the assistant tool layer with `search_erp_records`, using the existing Supabase/RLS-backed global search path for central parties, branches, stock, invoices, platform billing documents, and manufacturing tags/barcodes. Assistant answers still remain deterministic and draft-only for risky actions, with best-effort `assistant_action_audit`; verified `npx tsc --noEmit --pretty false` and active-source no-local/no-large-limit grep.
- Replaced active UI "Coming Soon" wording for deferred modules with explicit release-gate/not-enabled copy, and corrected Material Book availability flags so Meena/Polishing are visibly planned rather than pretending to be transactional. This keeps incomplete workflows honest instead of decorative; verified `npx tsc --noEmit --pretty false`.
- Added `src/lib/manufacturing-report-query.ts` and rewired `/reports/manufacturing` to load period+branch report rows directly from Supabase/RLS with exact count metadata, a 1000-row capped preview warning, retryable error state, loading state, and filtered-empty state. This removes another Reports path from compatibility-store filtering without adding browser-local storage. Verified `npx tsc --noEmit --pretty false` and active-source no-local/no-large-limit grep.
- Added `src/lib/gold-position-report-query.ts` and rewired `/reports/gold-position` to compute the five-bucket gold position from a bounded Supabase/RLS `gold_ledger` period+branch slice instead of the in-memory ledger compatibility store. The route now has retryable error handling, loading and empty states, and a capped-preview warning for large ledger periods. Verified `npx tsc --noEmit --pretty false`, active-source no-local/no-large-limit grep, `npm run build`, and unauthenticated `/reports/gold-position` Playwright smoke with no blank/database-error/console-error result.
- Lowered the generic Supabase repository `readAll()` compatibility escape hatch from 50,000 rows to 1,000 rows and documented it as transitional only. High-volume screens still need route-specific Supabase query/RPC paths, but any remaining legacy caller is no longer allowed to pull a 50k tenant-wide table by default. Verified `npx tsc --noEmit --pretty false`, precise active-source grep for `.limit(50000)`, `.limit(10000)`, `.limit(20000)`, `.limit(5000)`, and browser-local storage APIs, plus `npm run build`.
- Added `supabase/migrations/20260813055000_ceo_branch_kpis_rpc.sql` with `get_ceo_branch_kpis(text)` and rewired `src/lib/services/ceo-dashboard-service.ts` so branch KPI cards use an RLS-respecting Supabase RPC first, with a bounded direct-Supabase fallback instead of `readAll()` across invoices, orders, job cards, repairs, and people. Cleaned stale commented Supabase query code from `src/routes/dashboard.ceo.tsx`. Verified `npx tsc --noEmit --pretty false`, `npm run build`, and unauthenticated `/dashboard/ceo` Playwright smoke with no blank/database-error/console-error result. The CEO dashboard remains PARTIAL because gold trend, manufacturing summary, production chart, and worker table still depend on bounded compatibility stores pending dedicated Supabase aggregate paths.
- Added `src/lib/services/ceo-dashboard-analytics.ts` and `supabase/migrations/20260813055500_ceo_dashboard_gold_trend_rpc.sql` so CEO gold trend, manufacturing gold summary, production status, and worker performance cards now load through bounded Supabase/RLS reads or an RLS-respecting gold trend RPC instead of ledger/job-card/manufacturing/worker compatibility stores. Verified `npx tsc --noEmit --pretty false`, active touched-path no-local-storage/no-huge-limit grep, `npm run build`, and unauthenticated `/dashboard/ceo` Playwright smoke with no blank/database-error/console-error result. Remote migration application is blocked in this shell because the checkout is linked to stale project `zbfbnwgbqydttsuuhmxn` and `npx supabase migration list --linked` returns `Resource has been removed`; the Product Owner-specified Supabase project is `dqgrrafuoxaorvyrcuuh`, so the migration file is prepared but not remotely applied from the stale link.
- Added `src/lib/gold-summary-report-query.ts` and rewired `/reports/gold-summary` plus `/reports/gold-outstanding` to bounded Supabase/RLS report reads with loading/error/empty/capped states, CSV/print actions, and no compatibility-store imports from manufacturing bills, job cards, or worker gold book. Verified `npx tsc --noEmit --pretty false`, focused no-store/no-local/no-huge-limit grep, `npm run build`, and unauthenticated Playwright smokes for both report routes with no blank/database-error/console-error result. These reports remain PARTIAL until authenticated no-leak evidence and full-history aggregate/export paths are available.
- Added `src/lib/home-dashboard-query.ts` and prepared `supabase/migrations/20260813060000_home_dashboard_summary_rpc.sql` so the daily Home dashboard is RPC-first and no longer imports tenant-wide Orders, Job Cards, Ledger, Stock, Billing, or People compatibility stores for its read-side cockpit metrics. The route now has skeleton, retryable error, and bounded-fallback warning states while preserving order buckets and WhatsApp reminder draft behavior from visible Supabase rows. Verified `npx tsc --noEmit --pretty false`, focused no-store/no-local/no-huge-limit grep for the touched route/query, `npm run build`, and unauthenticated `/` Playwright smoke with no blank/database-error/console-error result. Remote RPC application remains blocked by the stale linked Supabase project until the checkout is relinked/authenticated to `dqgrrafuoxaorvyrcuuh`.
- Renamed the workflow settings business-mode option from ambiguous "hybrid" wording to `combined_commerce_manufacturing` in active UI/config while preserving a read-time normalizer for old persisted Supabase rows. Runtime workflow state now exposes only retail-only, manufacturing-only, or combined commerce/manufacturing business modes, and the old string is written out cleanly on the next Supabase-backed settings save. Verified `npx tsc --noEmit --pretty false`; the only remaining active-source `hybrid` matches in the touched files are the legacy-value normalizer, not a local/offline architecture path.
- Added `src/lib/billing-documents-query.ts` and rewired the Credit Notes, Debit Notes, Estimates, and Delivery Challans registers to bounded Supabase/RLS reads for their lists and selector data instead of depending on broad billing/people/order compatibility-store hydration. Create/update actions still use the existing Supabase-backed document stores, and the visible registers update locally after create/cancel/convert. Verified `npx tsc --noEmit --pretty false` and focused scans showing the touched billing document routes no longer import `useBilling`, `usePeople`, `useOrders`, or `readAll()`.
- Extended the assistant tool layer with `get_document_register_summary`, backed by the same bounded Supabase/RLS billing document query path. The assistant can now answer document/printing register questions for visible credit notes, debit notes, estimates, and delivery challans without fabricating counts or pulling broad compatibility stores. Verified `npx tsc --noEmit --pretty false` and focused assistant no-local/readAll grep.
- Added `src/lib/branch-report-query.ts` and rewired `/reports/branch` so Branch Performance KPIs, chart data, and exports come from bounded Supabase/RLS reads across invoices, orders, and repairs instead of client compatibility stores. The route now has loading and retryable error states while preserving period/branch filters and CSV/XLSX export. Verified `npx tsc --noEmit --pretty false` and a focused no-store/no-local/no-huge-limit grep for the touched report path.
- Added `src/lib/daily-close-query.ts` and rewired `/reports/daily-close` read-side snapshot calculation to bounded Supabase/RLS reads across gold ledger, invoices, job cards, repairs, orders, inventory, and worker withdrawals. The route keeps the existing Supabase-backed close save/print flow, now adds loading/error/capped-preview states, and no longer hydrates the main ledger/order/job/stock/billing/repair/worker stores to calculate the visible close snapshot. Verified `npx tsc --noEmit --pretty false` and focused scans showing only intentional domain calculator imports remain in the query helper.
- Added `src/lib/delivery-summary-query.ts` and rewired `/reports/delivery-summary` so the internal dealer/delivery-person reconciliation report reads delivered customer settlements and linked invoices directly from Supabase/RLS for the selected date instead of importing the customer-settlement and billing compatibility stores. The screen now includes loading, retryable error, and capped-preview states while preserving print/CSV output and existing settlement financial calculations. Verified `npx tsc --noEmit --pretty false` and a focused no-store scan for the touched route/helper; remaining production follow-up is promoting `delivered_at` to an indexed column/RPC for high-volume delivery-day evidence.
- Extended the same Supabase/RLS settlement report path to `/reports/settlements` and `/reports/settlement-reconciliation`: both reports now load bounded customer settlement rows and linked invoices directly from Supabase, include loading/error/retry/capped states, and no longer import `useSettlements` or `useBilling` for their visible read side. Verified `npx tsc --noEmit --pretty false` and a focused no-store/no-local scan for the touched reports.
- Added `src/lib/party-report-query.ts` and rewired `/reports/dealer` plus `/reports/worker` so bullion/vendor balances, karigar pending gold/quantity, and last settlement dates are calculated from bounded Supabase/RLS People, gold settlement, and worker transaction reads instead of hydrating People, Worker Gold Book, and Gold Settlement stores in the report routes. Both routes now expose loading/error/retry/capped states. Verified `npx tsc --noEmit --pretty false` and focused no-store/no-local scans for the touched party reports.
- Added single-invoice Supabase/RLS loading to `src/lib/billing-query.ts` and rewired `/billing/$id` so invoice detail/print/payment/cancel pages no longer show a false "Invoice not found" when reached by direct link before the invoice register cache is hydrated. The page now fetches the invoice and customer contact directly by ID, seeds only that invoice into the existing Supabase-backed billing store for write actions, and shows loading/retryable error states. Verified `npx tsc --noEmit --pretty false` and focused no-People-store/no-local scans for the touched invoice detail path.
- Added `src/lib/use-billing-invoice.ts` and rewired `/billing/print/$id`, `/billing/receipt/$id`, and `/billing/settlement-slip/$id` so invoice print, receipt print, and customer settlement slip direct links fetch the invoice by ID from Supabase/RLS and seed only that invoice into the print/runtime store before rendering. This removes another printing failure path where direct print pages could show "Invoice not found" unless the billing register had already hydrated. Verified `npx tsc --noEmit --pretty false` and focused scans showing no route-level invoice-list store lookup remains in the touched print routes.
- Added direct Supabase/RLS document loading through `fetchBillingDocumentById()` and `src/lib/use-billing-document.ts`, then rewired credit note, debit note, estimate, and delivery challan detail/print routes so direct links fetch exactly the requested document by ID and seed only that document into the existing Supabase-backed runtime stores for cancel/convert/return actions. This removes another print/detail failure path where document pages depended on the full document register being hydrated first. Verified `npx tsc --noEmit --pretty false` and focused scans showing the touched document detail/print routes no longer use full-register route lookups, huge limits, or browser-local storage.
- Replaced receipt/document verification number lookup full-table scans in `src/lib/services/receipt-record-service.ts` with direct Supabase/RLS column and JSON-field lookups for job cards, orders, invoices, repairs, and rate-cut slips. The `/verify` QR/document-number path no longer calls generic `readAll()` to find a receipt by number. Verified `npx tsc --noEmit --pretty false` and focused no-`readAll`/no-local/no-huge-limit grep for the verification service.
- Added `src/lib/use-gold-settlement-record.ts` and rewired `/billing/gold-settlement-print/$id` so gold settlement vouchers fetch the requested settlement and party by ID from Supabase/RLS, then seed only those records into runtime stores before rendering the existing voucher layout. The route no longer imports the full People store directly for voucher party details. Verified `npx tsc --noEmit --pretty false` and focused no-local/no-huge-limit scans for the touched voucher print path.
- Removed the remaining billing-document store `readAll()` refresh paths: credit notes, debit notes, estimates, and delivery challans now refresh through the bounded Supabase/RLS document query helpers used by their registers. The legacy `/billing/estimate/$id` route now uses `useBillingDocumentById("estimate", id)` instead of a preloaded estimate store lookup, so old estimate direct links get loading/retryable error states and do not depend on register hydration. Verified `npx tsc --noEmit --pretty false` and focused scans showing no billing-document repository `readAll()` refresh remains.
- Replaced saved filter refresh `readAll()` with a direct bounded Supabase/RLS read in `src/lib/saved-filters-store.ts`, capped to the latest 250 saved views by `updated_at`. Save/delete still use the existing Supabase repository write path. Verified `npx tsc --noEmit --pretty false` and focused no-`readAll`/no-local/no-huge-limit scan for the saved-filter store.
- Added the Product Owner Supabase MCP server for project `dqgrrafuoxaorvyrcuuh`, authenticated it, installed the Supabase agent skills, and applied the prepared dashboard/large-data RPC migrations remotely: `internal_helper_rpc_tightening`, `missing_foreign_key_indexes`, `platform_firm_stats_rpc`, `billing_outstanding_rpc`, corrected JSON-branch `ceo_branch_kpis_rpc`, `ceo_dashboard_gold_trend_rpc`, and `home_dashboard_summary_rpc`. Supabase MCP verification shows the expected read RPCs exist as `security invoker`, sensitive entitlement helper RPCs are service-role only, public tables without RLS = 0 of 129, RLS-enabled public tables without policies = 0, and remaining unindexed FK findings are limited to Supabase-managed `auth`/`storage` schemas rather than Ornexa public tables.
- Corrected local migration `supabase/migrations/20260813055000_ceo_branch_kpis_rpc.sql` to match the live Supabase schema by deriving branch scope from JSON `data->>'branchId'` and declaring the function `security invoker`; the original physical `branch_id` reference failed against the target project because those tables do not expose a physical `branch_id` column.
- Added `src/lib/outside-work-query.ts` and rewired Outside Work transaction/labour/payment refreshes to bounded, ordered Supabase/RLS reads instead of generic repository `readAll()` compatibility refreshes. The Outside Work page now also surfaces overdue external-work follow-up count from expected return dates and outstanding fine-gold position, keeping the manufacturing traceability question "who has our gold and since when?" visible without adding browser-local storage. Verified `npx tsc --noEmit --pretty false` and focused no-`readAll`/no-local/no-huge-limit scan for the touched outside-work files.
- Added `src/lib/financial-lock-query.ts` and rewired financial-lock loading/enforcement to bounded Supabase/RLS reads instead of `lockRepository.readAll()`. Ledger postings, worker gold-book postings, and expense postings now use `assertPeriodOpenOnline()` to check the exact branch+period lock before writing, rather than refreshing the full lock compatibility cache first. Verified `npx tsc --noEmit --pretty false` and focused financial-lock no-`readAll` scan.
- Added `src/lib/hallmark-query.ts` and `src/lib/physical-stock-verification-query.ts`, then rewired Hallmark batch and Physical Stock Count loading to bounded, ordered Supabase/RLS reads instead of generic repository `readAll()` compatibility refreshes. Hallmark and stock-count writes still use the existing Supabase-backed repository save path. Verified `npx tsc --noEmit --pretty false`, `npm run build`, active-source no-local/no-huge-limit grep, and focused no-`readAll` scans for the touched stores.
- Added `src/lib/lot-query.ts` and `src/lib/stone-query.ts`, then rewired Stock Lot, legacy Lot Batch, Stock Stone, and legacy Stone Detail loading to bounded, ordered Supabase/RLS reads instead of generic repository `readAll()` compatibility refreshes. This keeps jewellery lot/stone traceability on the Supabase-online path while preserving current write behavior. Remaining release work is consolidating the old/new lot and stone shapes into the central inventory foundation with migration and authenticated no-leak evidence.
- Added `src/lib/custody-flow-query.ts` and rewired Customer Gold Deposit, Material Vault, Metal Conversion, Polishing, Customer Settlement, Worker Return, and Workshop Process refreshes to bounded Supabase/RLS reads instead of generic repository `readAll()` compatibility refreshes. Physical gold/material custody flows now avoid broad tenant hydration for these stores while preserving existing Supabase-backed write paths. Verified `npx tsc --noEmit --pretty false` and focused scans showing the old custody-flow `readAll()` calls, browser-local APIs, and huge limits are absent from active source.
- Added `src/lib/print-engine/template-query.ts` and rewired the unified print template store to load saved template overrides through bounded Supabase/RLS reads instead of generic repository `readAll()`. Print template create/update/delete still use the existing Supabase-backed repository write path, and shipped defaults remain in-memory fallbacks when no override exists.
