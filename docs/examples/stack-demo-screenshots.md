# Stack Demo Screenshots

This documentation-only file is the third PR in the safe example stack.

It depends on `docs/example-stack-checklist`, which depends on `docs/example-stack-foundation`.

Screenshot guidance:

- UI PRs attach screenshots.
- Docs-only PRs may write `N/A`.
- Child PR screenshots should be refreshed if a parent PR changes visible behavior.
