# Ornexa QA — copy to qa/config/qa.env (gitignored) or export before running qa:*
# NEVER point active/destructive suites at production.

QA_BASE_URL=http://localhost:3000
E2E_BASE_URL=http://localhost:3000

# Dedicated QA Supabase project (test/staging only)
QA_SUPABASE_URL=https://dqgrrafuoxaorvyrcuuh.supabase.co
QA_SUPABASE_ANON_KEY=

# Tenant A / B isolation (seeded by qa/scripts/seed-qa-tenants.mjs)
QA_TENANT_A_FIRM_ID=
QA_TENANT_B_FIRM_ID=
QA_BRANCH_A1_ID=
QA_BRANCH_A2_ID=

# Role test accounts (passwords in secrets manager — not committed)
QA_OWNER_A_EMAIL=
QA_OWNER_A_PASSWORD=
QA_KARIGAR_A_EMAIL=
QA_KARIGAR_A_PASSWORD=
QA_CUSTOMER_A_EMAIL=
QA_CUSTOMER_A_PASSWORD=
QA_SUPPLIER_A_EMAIL=
QA_SUPPLIER_A_PASSWORD=

# Razorpay TEST MODE only
RAZORPAY_KEY_ID=rzp_test_REPLACE

# Strix AI pentest (staging only — requires human review of findings)
STRIX_LLM=gemini/gemini-3.6-flash
# LLM_API_KEY=set-in-user-env-not-committed
STRIX_MAX_BUDGET=3
STRIX_REASONING_EFFORT=medium

# QA run tagging
QA_RUN_ID=
QA_ALLOW_PRODUCTION_TARGET=0
