/**
 * Public self-signup is ENABLED with a 3-Day Free All-Access Evaluation Trial.
 * New jewellery businesses can register and evaluate ERP features immediately.
 */
export const PUBLIC_SELF_SIGNUP_DISABLED = false;

export const DEFAULT_PUBLIC_TRIAL_DAYS = 3;

export const PUBLIC_SIGNUP_PATH = "/signup" as const;
export const REQUEST_ACCESS_PATH = "/signup" as const;

/** Trial URL — routes to the 3-day public signup page. */
export const LEGACY_TRIAL_PATH = "/signup" as const;
