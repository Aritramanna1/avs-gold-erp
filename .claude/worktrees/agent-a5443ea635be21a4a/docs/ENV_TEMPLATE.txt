# ---------------------------------------------------------------------------
# MTJ ERP — environment template (Option B: external Supabase, no Lovable Cloud)
# Copy to `.env` at the project root. Never commit a populated .env.
# Production backend project ref: kjfjsfhftytezsjyegmb
# ---------------------------------------------------------------------------

# === Client-visible (safe to ship to the browser AND inside Electron) ===
VITE_SUPABASE_URL="https://kjfjsfhftytezsjyegmb.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_fThRlMsK8N5t_wU9_fzd7g_XBvvr-zW"
VITE_SUPABASE_PROJECT_ID="kjfjsfhftytezsjyegmb"

# === Server-only mirror (read by TanStack server functions / SSR) ===
# Same publishable key, just named without VITE_ for server runtime.
SUPABASE_URL="https://kjfjsfhftytezsjyegmb.supabase.co"
SUPABASE_PUBLISHABLE_KEY="sb_publishable_fThRlMsK8N5t_wU9_fzd7g_XBvvr-zW"
SUPABASE_PROJECT_ID="kjfjsfhftytezsjyegmb"

# === STRICT SECRETS — never put real values in this file, never ship in any client ===
# SUPABASE_SERVICE_ROLE_KEY=  # set ONLY in host secret manager (Horizon / VPS env)
# SUPABASE_DB_URL=            # set ONLY in host secret manager; never bundled

# === Electron desktop shell ===
# URL the desktop window loads. Set to your deployed Horizon URL (or custom domain).
# MTJ_ERP_URL="https://erp.maatara.example"
